{
  "name": "cafe-experience-next",
  "version": "1.0.0",
  "private": true,
  "description": "Cafe Experience Tracker — Next.js frontend + Express backend (one-terminal runner)",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently -n backend,frontend -c green,cyan \"npm run dev --prefix backend\" \"NEXT_PUBLIC_API_URL=http://localhost:5002 npm run dev --prefix frontend -- --port 3001\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "NEXT_PUBLIC_API_URL=http://localhost:5002 npm run dev --prefix frontend -- --port 3001",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
